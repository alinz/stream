{
  "name": "@stream/parser",
  "version": "1.0.0",
  "browserslist": [
    "last 1 Chrome version"
  ],
  "main":"./build/index.js",
  "lib": "./build/index.js",
  "typings": "./build",
  "scripts": {
    "test": "jest",
    "clean": "rm -rf build",
    "build:module": "parcel build src/index.ts --no-cache -d build -t node",
    "build:types": "tsc --outDir build -p .",
    "build": "yarn clean; yarn build:module; yarn build:types"
  },
  "dependencies": {
    "@stream/lexer": "1.0.0"
  },
  "devDependencies": {
    "@types/jest": "24.0.12",
    "@types/node": "12.0.0",
    "babel-core": "6.26.3",
    "babel-plugin-root-import": "6.1.0",
    "jest": "24.8.0",
    "parcel-bundler": "1.12.3",
    "ts-jest": "24.0.2",
    "typescript": "3.4.5"
  }
}
